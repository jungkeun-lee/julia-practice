function count(word, letter, index)
    counter = 0
    for i in index:length(word)
        if word[i] == letter
            counter += 1
        end
    end
    return counter
end
println(count("banana", 'a', 4))

for i in 1:5
    println(i)
end
